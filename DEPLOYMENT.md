# 🚀 Инструкция по деплою Mikkeller Running Club на ваш сервер

## Что такое CapRover?

CapRover — это бесплатная платформа для деплоя приложений с веб-интерфейсом. Вы получите:
- ✅ Простое управление через браузер (без работы в терминале)
- ✅ Автоматический SSL сертификат для HTTPS
- ✅ Простое обновление приложения
- ✅ Логи и мониторинг в реальном времени
- ✅ Автоматические резервные копии

---

## 📋 Что вам понадобится

1. **Сервер (VPS)** с Ubuntu 20.04 или новее
2. **IP адрес** сервера
3. **Пароль root** или sudo пользователя
4. **Доменное имя** (например, mikkellerrc.ru)
5. **15-20 минут** свободного времени

## ✅ Решенные проблемы

- **Production Build Issue ИСПРАВЛЕНА**: Приложение теперь корректно собирается для production. Использован динамический импорт с computed path для предотвращения включения Vite в production bundle.
- Оптимизированный Docker образ использует multi-stage build и содержит только необходимые production зависимости.

---

## Часть 1: Установка CapRover (одноразовая настройка)

### Шаг 1: Подключение к серверу

**Вариант A: Через SSH клиент (рекомендуется для Windows)**

1. Скачайте [PuTTY](https://www.putty.org/) (бесплатно)
2. Запустите PuTTY
3. В поле "Host Name" введите IP адрес вашего сервера
4. Нажмите "Open"
5. Введите логин: `root`
6. Введите ваш пароль

**Вариант B: Через терминал (Mac/Linux)**

Откройте терминал и выполните:
```bash
ssh root@ВАШ_IP_АДРЕС
```
Введите пароль когда попросит.

---

### Шаг 2: Настройка DNS (важно!)

**Перед установкой CapRover** нужно настроить DNS записи у вашего регистратора доменов:

Добавьте следующие A-записи:

| Тип | Имя | Значение (IP адрес) | TTL |
|-----|-----|---------------------|-----|
| A | captain.ваш-домен.ru | ВАШ_IP_АДРЕС | 3600 |
| A | *.captain.ваш-домен.ru | ВАШ_IP_АДРЕС | 3600 |

**Например, если ваш домен mikkellerrc.ru:**
- `captain.mikkellerrc.ru` → ваш IP
- `*.captain.mikkellerrc.ru` → ваш IP

⏰ **Подождите 5-10 минут** пока DNS записи обновятся.

---

### Шаг 3: Установка Docker (подготовка)

Скопируйте и вставьте эти команды **по очереди** в терминал:

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com | sh

# Проверка установки
docker --version
```

Вы должны увидеть версию Docker (например, "Docker version 24.0.7").

---

### Шаг 4: Установка CapRover

Скопируйте и выполните:

```bash
docker run -p 80:80 -p 443:443 -p 3000:3000 -v /var/run/docker.sock:/var/run/docker.sock -v /captain:/captain caprover/caprover
```

Через минуту вы увидите сообщение:
```
**** CapRover is available at http://captain.ваш-домен.ru ****
```

✅ CapRover установлен!

---

### Шаг 5: Первый вход в CapRover

1. Откройте браузер и перейдите на: `http://captain.ваш-домен.ru`
2. Вы увидите страницу входа CapRover
3. **Пароль по умолчанию:** `captain42`
4. Нажмите "Login"

**Сразу после входа:**
1. Перейдите в **Settings** (слева в меню)
2. В разделе "Change Password" установите **новый надежный пароль**
3. Сохраните этот пароль в надежном месте!

---

### Шаг 6: Включение HTTPS (SSL сертификат)

1. В меню слева выберите **Settings**
2. Прокрутите до раздела **"CapRover Root Domain Configurations"**
3. Нажмите кнопку **"Enable HTTPS"**
4. Введите ваш email (для Let's Encrypt уведомлений)
5. Нажмите **"Enable HTTPS"**

⏰ Подождите 1-2 минуты. Страница автоматически перезагрузится и откроется через HTTPS.

✅ Теперь CapRover защищен SSL сертификатом!

---

## Часть 2: Создание базы данных PostgreSQL

### Шаг 7: Установка PostgreSQL в CapRover

1. В меню слева выберите **"One-Click Apps/Databases"**
2. Найдите **"PostgreSQL"** в списке
3. Нажмите на PostgreSQL
4. Заполните форму:
   - **App Name:** `mikkeller-db`
   - **Postgres Password:** придумайте надежный пароль (сохраните его!)
   - **Postgres Version:** оставьте последнюю версию (например, 15)
5. Нажмите **"Deploy"**

⏰ Подождите 1-2 минуты пока база данных установится.

---

### Шаг 8: Создание базы данных и пользователя

1. В меню слева выберите **"Apps"**
2. Найдите приложение **"mikkeller-db"** и кликните на него
3. Прокрутите вниз до раздела **"Deployment"**
4. Нажмите кнопку **"Execute Command"**
5. В поле введите:
   ```bash
   psql -U postgres
   ```
6. Нажмите **"Execute"**
7. В появившемся окне терминала выполните **по очереди**:

```sql
CREATE DATABASE mikkeller_rc;
CREATE USER mikkeller_user WITH PASSWORD 'ваш_надежный_пароль';
GRANT ALL PRIVILEGES ON DATABASE mikkeller_rc TO mikkeller_user;
\c mikkeller_rc
GRANT ALL ON SCHEMA public TO mikkeller_user;
\q
```

**Важно:** Замените `'ваш_надежный_пароль'` на реальный пароль и **сохраните его**!

✅ База данных готова!

---

## Часть 3: Деплой приложения Mikkeller Running Club

### Шаг 9: Создание приложения в CapRover

1. В меню слева выберите **"Apps"**
2. Нажмите **"Create New App"**
3. В поле **"App Name"** введите: `mikkeller-rc`
4. Нажмите **"Create New App"**

---

### Шаг 10: Настройка переменных окружения

1. Найдите приложение **"mikkeller-rc"** в списке и кликните на него
2. Прокрутите до раздела **"App Configs"**
3. В разделе **"Environmental Variables"** добавьте следующие переменные:

Нажмите **"Add Key Value"** и добавьте каждую переменную:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `DATABASE_URL` | `postgresql://mikkeller_user:ВАШ_ПАРОЛЬ@srv-captain--mikkeller-db:5432/mikkeller_rc` |
| `SESSION_SECRET` | Любая случайная строка минимум 32 символа* |
| `VITE_YANDEX_MAPS_API_KEY` | Ваш ключ Yandex Maps API |

**Генерация SESSION_SECRET:**
Откройте [этот генератор](https://randomkeygen.com/) и скопируйте любую длинную строку из раздела "Fort Knox Passwords".

**Важно:** В `DATABASE_URL` замените `ВАШ_ПАРОЛЬ` на пароль который вы создали для mikkeller_user в Шаге 8.

После добавления всех переменных нажмите **"Save & Update"**.

---

### Шаг 11: Настройка персистентного хранилища для файлов

Файлы (фото, обложки, GPX) должны сохраняться между обновлениями приложения.

1. В том же приложении **"mikkeller-rc"** прокрутите до раздела **"Persistent Directories"**
2. Нажмите **"Add Persistent Directory"**
3. Заполните:
   - **Path in App:** `/app/server/uploads`
   - **Label:** `uploads`
4. Нажмите **"Save & Update"**

---

### Шаг 12: Включение HTTPS для приложения

1. В разделе **"HTTP Settings"**
2. Поставьте галочку **"Enable HTTPS"**
3. Поставьте галочку **"Force HTTPS by redirecting all HTTP traffic to HTTPS"**
4. Нажмите **"Save & Update"**

---

### Шаг 13: Подготовка файлов для деплоя

На вашем компьютере:

1. В Replit нажмите меню ☰ (три линии) в левом верхнем углу
2. Выберите **"Download as zip"**
3. Сохраните архив на компьютер
4. **Распакуйте** архив в папку (например, `mikkeller-rc`)

---

### Шаг 14: Деплой приложения через CapRover CLI

**Вариант A: Через Web Interface (проще)**

1. В CapRover откройте приложение **"mikkeller-rc"**
2. Прокрутите до раздела **"Deployment"**
3. Перейдите на вкладку **"Upload tarball or zip"**
4. Нажмите **"Choose File"** и выберите скачанный zip файл
5. Нажмите **"Upload & Deploy"**

⏰ Подождите 5-10 минут пока приложение соберется и запустится.

**Вариант B: Через CapRover CLI (для продвинутых)**

Если вы хотите деплоить обновления быстрее в будущем:

1. Установите Node.js на вашем компьютере ([скачать](https://nodejs.org/))
2. Откройте терминал/командную строку
3. Установите CapRover CLI:
   ```bash
   npm install -g caprover
   ```
4. Перейдите в папку с проектом:
   ```bash
   cd путь/к/mikkeller-rc
   ```
5. Войдите в CapRover:
   ```bash
   caprover login
   ```
   - URL: `https://captain.ваш-домен.ru`
   - Password: ваш пароль от CapRover
   - Name: `mikkeller-rc`
6. Деплой:
   ```bash
   caprover deploy
   ```
   - App Name: `mikkeller-rc`

---

### Шаг 15: Применение миграций базы данных

После первого деплоя нужно создать таблицы в базе данных:

1. В CapRover откройте приложение **"mikkeller-rc"**
2. Прокрутите до раздела **"Deployment"**
3. Нажмите **"Execute Command"**
4. Введите:
   ```bash
   npm run db:push
   ```
5. Нажмите **"Execute"**

⏰ Подождите минуту. Вы увидите список созданных таблиц.

✅ База данных настроена!

---

### Шаг 16: Создание первого администратора

1. В том же окне **"Execute Command"** выполните:
   ```bash
   node dist/index.js --create-admin
   ```

**Альтернативный способ (через psql):**

1. Откройте приложение **"mikkeller-db"**
2. Нажмите **"Execute Command"**
3. Введите:
   ```bash
   psql -U mikkeller_user -d mikkeller_rc
   ```
4. Введите пароль когда попросит
5. Выполните SQL:
```sql
INSERT INTO admins (username, password) 
VALUES ('admin', '$2a$10$YourHashedPasswordHere');
```

**Проще всего:** Зайдите на ваш сайт и используйте функцию регистрации первого админа, если она реализована.

---

### Шаг 17: Настройка домена для приложения

1. В приложении **"mikkeller-rc"** перейдите в раздел **"HTTP Settings"**
2. В поле **"Custom Domain"** введите ваш домен (например, `mikkellerrc.ru`)
3. Нажмите **"Connect New Domain"**
4. Поставьте галочку **"Enable HTTPS"**
5. Нажмите **"Save & Update"**

**Не забудьте настроить DNS у регистратора:**

Добавьте A-запись:
- Имя: `@` (или оставьте пустым)
- Тип: `A`
- Значение: IP адрес вашего сервера
- TTL: 3600

⏰ Подождите 5-10 минут для применения DNS.

---

## 🎉 Готово!

Ваше приложение доступно по адресу:
- **https://ваш-домен.ru**
- **https://mikkeller-rc.captain.ваш-домен.ru** (техническое имя)

---

## 📱 Как обновлять приложение в будущем

### Способ 1: Через Web Interface
1. Скачайте новую версию проекта из Replit
2. В CapRover откройте **"mikkeller-rc"** → **"Deployment"**
3. Загрузите новый zip файл
4. Нажмите **"Upload & Deploy"**

### Способ 2: Через CLI (быстрее)
```bash
cd путь/к/mikkeller-rc
caprover deploy
```

---

## 🔧 Полезные команды и инструменты

### Просмотр логов
1. Откройте **"mikkeller-rc"** в CapRover
2. Перейдите на вкладку **"App Logs"**
3. Вы увидите логи в реальном времени

### Перезапуск приложения
1. Откройте **"mikkeller-rc"**
2. Нажмите **"Save & Update"** (это перезапустит приложение)

### Резервная копия базы данных
1. Откройте **"mikkeller-db"**
2. Нажмите **"Execute Command"**
3. Введите:
```bash
pg_dump -U mikkeller_user mikkeller_rc > backup.sql
```

### Восстановление из бэкапа
```bash
psql -U mikkeller_user mikkeller_rc < backup.sql
```

---

## ❓ Часто задаваемые вопросы

**Q: Что делать если приложение не запускается?**
A: Проверьте логи в разделе "App Logs". Чаще всего проблема в неправильных переменных окружения.

**Q: Как добавить больше памяти приложению?**
A: В настройках приложения в разделе "App Configs" увеличьте "Instance Count" или "Memory Limit".

**Q: Файлы пропадают после обновления**
A: Убедитесь что вы настроили "Persistent Directories" для `/app/server/uploads`.

**Q: Как изменить порт приложения?**
A: CapRover автоматически пробрасывает порт 5000 на 80/443. Менять не нужно.

**Q: SSL сертификат не работает**
A: Убедитесь что DNS записи настроены правильно и прошло достаточно времени (до 10 минут).

---

## 📞 Поддержка

Если что-то не получается:
1. Проверьте логи приложения
2. Убедитесь что все переменные окружения заданы правильно
3. Проверьте что DNS записи настроены корректно
4. Попробуйте перезапустить приложение

Документация CapRover: https://caprover.com/docs/

---

## 🎯 Чеклист успешного деплоя

- ✅ Docker установлен
- ✅ CapRover установлен и доступен
- ✅ HTTPS включен для CapRover
- ✅ DNS записи настроены
- ✅ PostgreSQL база данных создана
- ✅ Приложение создано в CapRover
- ✅ Переменные окружения заданы
- ✅ Persistent storage настроен
- ✅ Приложение задеплоено
- ✅ Миграции применены
- ✅ Администратор создан
- ✅ Домен подключен
- ✅ SSL сертификат работает

**Поздравляем! Ваш сайт Mikkeller Running Club работает! 🎉**
